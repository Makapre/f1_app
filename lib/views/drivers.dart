import 'package:flutter/material.dart';

class DriversWidget extends StatelessWidget {
  final Color color;

  DriversWidget(this.color);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}
